from pygame importo midi

class Editor:
    """
    Class that manage the editor
        for composing the musics
    """
    def __init__(self, pygame_editor, stream_char = '', cursor_pos=0, stream_selected):
        self.stream_char = stream_char
        self.cursor_pos = cursor_pos
        self.stream_selected = stream_selected
        
    def copy();
        """ Copy the selected text and save it in the clipboard """
        pass
    
    def cut():
        """ Copy the selected text and delete it """
        pass
    
    def paste():
        """ Paste the text on the clipboard on the cursor position """
        pass
